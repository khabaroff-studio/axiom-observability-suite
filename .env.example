# ── Vector (все серверы) ──────────────────────────────────────────────────────
AXIOM_TOKEN=               # ingest-токен, права: Ingest
AXIOM_DATASET=prod-docker-logs
HOSTNAME=                  # уникальное имя сервера, напр. prod-server-1

# ── health-watcher (все серверы) ─────────────────────────────────────────────
UNHEALTHY_ALERT_DELAY_SECONDS=120  # алертить если нездоров дольше N секунд

# ── alertbot (только один сервер) ────────────────────────────────────────────
# Раскомментировать и заполнить только на том сервере, где будет alertbot.
# Роутинг алертов по тредам: routes.yml (копировать из routes.yml.example).

# COMPOSE_PROFILES=alertbot   # раскомментировать чтобы включить alertbot
# TELEGRAM_BOT_TOKEN=
# WEBHOOK_SECRET=

# health-watcher fallback: прямая отправка в Telegram, когда alertbot недоступен.
# TELEGRAM_CHAT_ID=
# TELEGRAM_TOPIC_ID=

# AXIOM_MGMT_TOKEN=          # PAT для axiom_cli.py, права: Monitors/Notifiers CRU, Datasets/Queries R
# AXIOM_ATTACH_INTERVAL_SECONDS=300  # интервал авто-привязки notifiers (alertbot)
# ALERTBOT_INCLUDE_RESOLVED=false     # отправлять resolved-события (по умолчанию нет)
# AXIOM_QUERY_BASE=https://cloud.axiom.co  # база для Axiom-запросов (enrichment)
